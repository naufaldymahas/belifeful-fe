import DefaultLayout from '@layouts/Default.layout';

const Cart = () => {
  return (
    <DefaultLayout>
      <div style={{ paddingTop: '10rem' }}>
        <span>ini page cart</span>
        <button className="btn btn-gold">Primary Button</button>
      </div>
    </DefaultLayout>
  );
};

export default Cart;
